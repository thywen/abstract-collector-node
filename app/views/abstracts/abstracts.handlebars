{{#each abstracts}}
    <div class="card card-body">
        <h4>{{title}}</h4>
        <p>{{details}}</p>
        <a href="/abstracts/edit/{{id}}" class="btn btn-dark btn-block">Edit</a>
    </div>
{{else}}
<p>No abstracts listed</p>
{{/each}}